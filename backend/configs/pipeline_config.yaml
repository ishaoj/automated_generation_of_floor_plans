gnn:
  # Path is resolved relative to the project root (vastu-ai/).
  # Override with GNN_MODEL_PATH environment variable on any server.
  model_path: "ml/models/saved/gnn_v3/best_model.pt"

inference:
  device: "auto"   # auto selects: cuda > mps > cpu
